Java 中的 Young GC（Minor GC）通常在以下条件下触发：

1. **Eden 区满**：当新生代的 Eden 区被填满时，系统会自动启动 Young GC 来回收不再使用的对象。

2. **对象分配失败**：当尝试在 Eden 区分配新对象但没有足够空间时，触发 Young GC。

3. **设置的阈值**：一些 JVM 参数可能影响垃圾回收的行为，如果内存使用达到某些阈值，也可能会触发 Young GC。

Young GC 主要用于快速回收短命对象，因此通常相对高效。你还有其他相关问题吗？
