Java中`ConcurrentHashMap`的`get`方法不需要加锁。它是线程安全的，因为`ConcurrentHashMap`采用了分段锁（在JDK 7及之前）或无锁的算法（在JDK 8及之后）来确保并发访问的安全性。具体来说：

- **JDK 7及之前**：`ConcurrentHashMap`将数据分为多个段，每个段使用独立的锁。当一个线程读取一个段的数据时，不会阻塞其他段的读操作。
  
- **JDK 8及之后**：`ConcurrentHashMap`在读取数据时使用了无锁算法，允许多个线程同时访问，而不需要锁定整个结构，极大地提高了性能。

因此，`get`方法在高并发场景下也能高效执行，而不会造成性能瓶颈。
