`TreeMap`是Java中的一个集合类，属于`SortedMap`接口的实现，主要特点包括：

1. **有序性**：`TreeMap`根据键的自然顺序（或构造时提供的比较器）自动对键进行排序，因此其元素总是按升序排列。

2. **基于红黑树**：`TreeMap`的实现基于红黑树，这是一种自平衡的二叉搜索树，能够在O(log n)的时间复杂度内完成插入、删除和查找操作。

3. **支持范围操作**：`TreeMap`提供了许多方法来处理键的范围，例如`subMap()`、`headMap()`和`tailMap()`，方便对键进行范围查询。

4. **允许null键**：与`HashMap`不同，`TreeMap`不允许使用null作为键，但可以使用null作为值。

5. **适用场景**：`TreeMap`适合用于需要有序集合和范围查询的场景，如实现排序的字典、索引等。

总的来说，`TreeMap`提供了一种有序的键值对存储方式，结合了高效的查找性能和灵活的范围查询能力。
